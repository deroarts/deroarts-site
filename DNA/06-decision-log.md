# 06 — Decision log

Registro delle decisioni tecniche rilevanti. Una riga per decisione: data · decisione · motivo. Aggiungere in coda, non riscrivere lo storico.

| Data | Decisione | Motivo |
|------|-----------|--------|
| 2026-07-02 | Rotta admin `/admin` → `/admina` | Scelta del proprietario; aggiornato codice, middleware, robots, DNA. |
| 2026-07-02 | RLS abilitata deny-by-default su tutte le tabelle (migrazione `enable_rls`) | Sicurezza enterprise; l'app usa Prisma come owner (bypassa RLS), l'anon REST resta bloccato. |
| 2026-07-02 | `force-dynamic` su tutte le route che leggono dal DB | Evitare il prerender al build (Render falliva: nessun DB / pool saturo). Build ora passa senza `DATABASE_URL`. |
| 2026-07-02 | Deploy Render standalone + `HOSTNAME=0.0.0.0` | Lo standalone Next si legava a localhost → 502. Bind su 0.0.0.0 risolve; `PORT` iniettato da Render. |
| 2026-07-02 | Primo deploy con `MAIL_MODE=fake` + `STORAGE_MODE=local` | Avvio rapido online; SMTP Zoho e Supabase Storage si implementano dopo (vedi [[05-deploy]]). |
| 2026-07-02 | DevSwitcher = strumento solo-dev, mai vincolato da auth/sicurezza | Passaggio rapido user/admin in sviluppo; gated da `DEV_UA_SWITCH`, rimosso in prod (vedi [[02-regole]]). |
| 2026-07-02 | Governance canonica in `AGENTS.md` che richiama `CLAUDE.md` + limite file 300 righe | Portabilità multi-agent; `CLAUDE.md` è universale e si riscarica dal sync, quindi le regole di progetto vivono in `AGENTS.md`. |
| 2026-07-02 | Keepalive Supabase via GitHub Actions ogni 2gg su tabella `ping` dedicata | Free tier pausa dopo ~7gg. Tabella `ping` (RLS SELECT-only anon) evita di esporre service_role; il ping tocca Postgres (auth/health non basta). |
| 2026-07-02 | Suite E2E/visual con Playwright in `tests/e2e/` | Verifica navigazione, flussi e regressioni visive; isolata (POST intercettati, nessuna scrittura DB), esclusa dal build. |
| 2026-07-02 | ESLint config + CI di qualità (typecheck/lint/build su push+PR) | Progetto online: prevenire regressioni. `next lint` era interattivo/non configurato (rompeva la CI). |
| 2026-07-02 | Integrazione brand kit v2.0: derivati (favicon multi-size, apple-touch, OG 1200x630, PWA icons) + manifest.webmanifest | Kit ufficiale in public/deroarts-brand-kit; risolve OG-in-SVG (crawler) e aggiunge PWA/favicon coerenti. |
| 2026-07-02 | Informativa privacy (/privacy) + cookie banner minimale (GDPR EU) | Il form raccoglie PII (nome/email); conformità EU prima del traffico reale. Nessun tracker; consenso semplice via localStorage. |
| 2026-07-02 | Next.js 14.2.35 + header sicurezza (HSTS, X-Frame, nosniff, Referrer, Permissions) | Patch ramo 14 + hardening. CVE DoS residui accettati (fix solo in major 15/16, app in sviluppo). |
| 2026-07-02 | U/A pill visibile in prod via NEXT_PUBLIC_SHOW_UA_SWITCH, bypass auth resta dev-only | Richiesta: pill sempre visibile. Separato dalla navigazione il bypass login (DEV_UA_SWITCH), mai attivo in prod → /admina resta protetto. |
| 2026-07-06 | `DEV_UA_SWITCH=true` attivato ANCHE in prod (Render + App Control) | Richiesta owner: la pill U/A deve entrare in /admina senza login anche sul sito live. ⚠️ /admina ora aperto a chiunque abbia l'URL finché il DevSwitcher non viene rimosso (a fine lavori). Supera la riga 2026-07-02 sopra. |
| 2026-07-06 | Dominio `www.deroarts.com` → Render (CNAME Cloudflare Solo-DNS) + apex `deroarts.com` 301→www | Go-live dominio proprio. Proxy off (Render verifica host + emette SSL). Apex via CNAME-flattening, MX/TXT Zoho non toccati. `NEXT_PUBLIC_SITE_URL` allineato a `https://www.deroarts.com`. |
| 2026-07-06 | Restyle home user: hero `HeroFlow` (griglia tecnica → linee → piuma-brand vettoriale) + font serif Fraunces | Togliere l'aspetto "template AI". Piuma = tracciato SVG del brand (`public/brand/deroarts-piuma-nera.svg`, path inline in `HeroFlow.tsx`), gradiente verde, fade-in dopo le linee. Solo sito user; admin invariato. |
| 2026-07-06 | Copy sito user: impersonale, generico, qualità↔prezzo accessibile | Regole owner: mai prima persona (né "creo"/"creiamo"), mai nominare destinatari (no imprese/PMI/privati), mai "artigianale"; qualità sempre legata al prezzo alla portata di tutti. "Contattaci"→"Contatti". |
| 2026-07-06 | `ScrollToTop` globale nel layout (scrollRestoration manual + scroll(0,0) su cambio pagina) | Ogni pagina/refresh deve partire dall'inizio in tutta l'app; il ripristino automatico del browser lasciava la vista a metà. |
| 2026-07-06 | Pulizia `public/`: rimossa `deroarts-brand-kit` (archiviata in BACKUP), tenuti solo asset usati | Da 3.4MB a ~0.9MB. Sito usa solo `public/brand/*`. Kit completo salvato in `BACKUP/brand-kit-archive/` (fuori git). |
| 2026-07-06 | Standard layout `.hero-screen` (globals.css) + allineamento spaziatura tutte le pagine | Hero riempie la prima schermata sotto l'header (`--header-h`), contenuto ancorato in alto → hero+CTA sempre visibili. Pagine allineate a `pt-8 pb-16`. Regola in [[03-architettura]]. |
| 2026-07-06 | Rimossi filtri categoria dalla pagina Progetti (+ componente `CategoryFilter`) | Richiesta owner: pagina più pulita/leggibile. Cover demo progetti azzerate (`cover_image_url=null`) → placeholder brand sobrio finché non si caricano screenshot reali. |
| 2026-07-06 | Agent API `POST/GET /api/agent/projects` + segreto `AGENT_API_KEY` | Integrazione agent esterno (Devin): legge contenuti pubblici + style_guide per contestualizzare, crea SOLO bozze (`published:false`), mai layout/pubblicazione/dati cliente. Auth via header `x-api-key` in-handler (rotta fuori dal middleware). Input sanificati (URL allowlist, status enum, slug de-dup). Chiave impostata su Render + App Control (scope Custom); testato live su prod (401/200/201 OK). Dettaglio in [[03-architettura]]. |
| 2026-07-06 | Token GitHub rimosso dall'URL del remote `origin` (era in chiaro) | Sicurezza: `git remote -v` esponeva `ghp_...` in chiaro nell'URL. URL ripulito a `https://github.com/deroarts/deroarts-site.git`; il token resta solo in `.env` (`GITHUB_TOKEN`) per l'auth al push. |
| 2026-07-06 | Audit privacy/sicurezza (sola lettura) — esito: rischio basso-medio | Verificato dal vivo: dati cliente (`requests`) non leggibili/scrivibili da anon (RLS deny + grant assenti), zero segreti nel bundle, no tracking. Aperti (non urgenti): `DEV_UA_SWITCH=true` in prod (voluto, /admina aperto), manca delete PII in admin, privacy notice cita Zoho non ancora attivo, no CSP, Next 14 con CVE. |
| 2026-07-06 | `SupabaseStorageAdapter` + bucket pubblico + `POST /api/agent/upload` | Immagini progetti permanenti: `STORAGE_MODE=supabase` carica su bucket pubblico `deroarts-assets` via REST (no `supabase-js`), URL CDN sopravvive ai redeploy (disco Render effimero). Nuovo endpoint upload per Devin (multipart, sharp, auth `x-api-key`): l'agent carica screenshot/loghi → riceve URL → crea bozza. `next.config` remotePatterns `*.supabase.co`. Testato live (upload→URL→bozza OK). `STORAGE_MODE=supabase` impostato su Render + bucket. Dettaglio in [[03-architettura]]. |
| 2026-07-06 | Consolidamento area Progetti (audit multi-agent, fix enterprise) | Fix: (1) placeholder `AspectImage` con dimensione fissa 48px (attr+inline style) → non esplode più senza CSS; (2) delete/update progetto ora puliscono le immagini orfane da Supabase (`storage.delete`, best-effort, testato: bucket svuotato); (3) `sort_order` NaN→0; (4) `moveProjectAction` reindicizza 0..n-1 con tie-break `created_at` (frecce sempre efficaci anche con ordini a 0); (5) clamp `?page` alla pagina max (no tabella vuota); (6) `revalidatePath('/progetti')` su toggle/delete/move; (7) `actions_json` validato come array; (8) rimossa prop morta `projectSlug`. Copy hero: "costruiti bene"→"costruiti ad hoc". |
| 2026-07-06 | `build:check` con distDir isolata (`.next-build`) | Il build di verifica corrompeva la cache `.next` del dev server (errore ricorrente "Cannot find module './xxx.js'"). Ora `NEXT_BUILD_DIR` → `distDir` separata: build e dev non si toccano più. `build` (Render) invariato. `.next-build` in `.gitignore`. Rimossi anche i `StatusBadge` dalle card della lista pubblica `/progetti`. |
| 2026-07-06 | `PATCH /api/agent/projects` (modifica senza doppioni) + cover=logo | Devin chiedeva modifiche → l'endpoint sapeva solo creare, generava doppioni. Nuovo PATCH: aggiorna per slug solo i campi passati, pulisce immagini sostituite, 404 se slug assente. `style_guide`: cover sempre = LOGO (screenshot in gallery). Prompt agent aggiornato (create+modifica, generico). Testato live: cover swap + cleanup OK. |
| 2026-07-06 | Editor testo ricco (Tiptap) sulle descrizioni progetti + sanitizzazione HTML | Descrizioni breve/lunga ora rich text (grassetto/corsivo/sottolineato/liste) via `RichTextEditor` (Tiptap, client) in `ProjectForm`, mirrorato in input nascosti. HTML sanificato server-side (`lib/sanitize-html.ts`, allow-list solo tag di formattazione) al salvataggio E in output pubblico (`dangerouslySetInnerHTML` + `prose` @tailwindcss/typography). Card lista/meta = strip-to-plain. Agent API: testo semplice di Devin → HTML sicuro (`plainTextToHtml`, a capo preservati). |
| 2026-07-06 | Rifiniture UI: tabella admin progetti + dettaglio pubblico | Solo presentazione. Tabella: cella "Progetto" cliccabile→edit, head bar verde scuro unico (`green-deep`), colonne centrate (tranne Progetto), icone azioni w-5 centrate, cestino rosso fisso, frecce riordino verde-deep, griglia v+h, rimossi sottotitoli. Dettaglio pubblico: badge stato rimosso (header + card), pulsanti azione senza box "Cosa puoi fare", spostati sotto la descrizione prima della galleria. |
| 2026-07-06 | Email reale via **Resend** (`ResendMailAdapter`, `MAIL_MODE=resend`) | Dominio `deroarts.com` Verified su Resend (account `dero975@gmail.com`, DKIM+SPF+MX via Cloudflare Auto-configure). Adapter accanto a `FakeMailAdapter` (switch invariato). Mittente = `from_email` del progetto (`stickers@…`) o `RESEND_FROM`. `replyTo` = email cliente (notifica) / indirizzo progetto (risposta). Locale resta `fake` (no invii nei test); prod: `MAIL_MODE=resend`. Dettaglio in [[08-messaggi-notifiche]]. |
| 2026-07-06 | Sezione admin **Messaggi** (evoluzione di Richieste) | `/admina/messaggi` con tabella stile admin (head bar `green-deep`, griglia, badge progetto, letto/non-letto/risposto), filtro per progetto + stato + ricerca, **card impilate su mobile** (nativa), dettaglio con **risposta da app** via Resend (segna risposto+gestito). Vecchia `/admina/richieste` → redirect (preserva link). Nav "Richieste"→"Messaggi". Fonte dati invariata (`requests`); la tabella NON legge Zoho (vedi §5bis di [[08-messaggi-notifiche]]). |
| 2026-07-06 | Migrazione DB additiva `messaggi_push_notifiche` (scritta a mano, `migrate deploy`) | Additiva: `requests.replied_at` (nullable) + tabelle `push_subscriptions` e `notification_settings` (RLS deny-by-default come le altre). Scritta a mano e applicata con `migrate deploy` perché `migrate dev` voleva DROPpare la tabella `ping` (keepalive, non in schema.prisma) — evitato. `ping` verificato intatto post-migrazione. |
| 2026-07-06 | Notifiche **push web** admin (attivabili/disattivabili) | Service worker `public/sw.js` + VAPID (`web-push`). Pannello in Impostazioni (`NotificationToggle`) attiva/disattiva per dispositivo (API `/api/admin/push`, iscrizioni in `push_subscriptions`, on/off in `notification_settings`). Nuovo messaggio dal sito → push a tutti i device attivi (`lib/push/send.ts`, auto-prune 404/410). Self-guarding: se VAPID mancano o push off, è no-op (non blocca mai email/salvataggio). iOS: solo se sito in Home. |
| 2026-07-06 | `DATABASE_URL` → pooler **Transaction mode** (6543) + `pgbouncer=true&connection_limit=1`; nuovo `DIRECT_URL` (5432) per le migrazioni | Deploy Render fallito con `FATAL (EMAXCONNSESSION) max clients reached in session mode, pool_size: 15`: il pooler Session (5432) + connessioni Prisma non limitate saturavano il pool free. Transaction mode + connection_limit=1 riduce drasticamente le connessioni (ideale serverless/Render free). `directUrl` in schema.prisma usa `DIRECT_URL` (Session) perché il pooler non supporta le migrazioni. Aggiornato in .env, App Control, Render. Nessun cambio di stack (resta Supabase). |
| 2026-07-06 | `build` esegue `prisma generate && next build` | Deploy Render falliva con `Type error: Property reply_to_email does not exist`: il build usava un client Prisma non rigenerato dopo l'aggiunta di campi. Ora la generazione è nel build (`build` e `build:check`), così Render ha sempre il client aggiornato. |
| 2026-07-07 | Casella unificata Zoho **abbandonata**: piano Zoho Free blocca IMAP, POP **e** Inoltro | Verificato dal vivo nelle impostazioni Zoho: tutte e tre le vie di "uscita automatica" sono a pagamento. Impossibile portare le mail di `info@` in app col piano gratuito. Il webhook Resend Inbound resta attivo (per app che scrivono all'indirizzo `.resend.app`). Le mail scritte a mano si leggono su Zoho. |
| 2026-07-07 | Endpoint **`POST /api/inbound/app`** per notifiche dalle app dei progetti | Via diretta e senza limiti (alternativa all'email): un'app manda una notifica → arriva in Messaggi collegata al progetto (slug). Auth con chiave dedicata `DEROARTS_APP_KEY` (separata da AGENT_API_KEY). Salva `source=email`, reply_to = from_email, scatta push. Testato: 401 senza chiave, 200 + messaggio collegato a stickers-matchbox, 400 senza message. Base del prompt Devin "Setup Mail DeroArts". Dettaglio in [[08-messaggi-notifiche]]. |
